# ── HTTP service ─────────────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
python-dotenv>=1.0
PyJWT>=2.8
python-multipart>=0.0.9

# ── Persistence (chunks, parent docs, graph store, GridFS file store) ────────
motor>=3.3

# ── RAG pipeline orchestration ────────────────────────────────────────────────
# No langchain-openai/anthropic/groq here — this service imports no provider
# SDK. langchain-core/-text-splitters/-community back the Document/Embeddings
# abstractions and the recursive text splitter; langgraph drives the
# ingestion/query state graphs. Pinned below 1.0 to match the LLM Gateway this
# service calls (its own langchain-core ceiling) — bump both together.
langgraph>=0.1,<1.0
langchain-core>=0.2,<1.0
langchain-community>=0.2,<1.0
langchain-text-splitters>=0.2,<1.0
rank-bm25>=0.2
numpy
scikit-learn

# ── PDF loading ────────────────────────────────────────────────────────────────
pypdf>=4.0
pdfplumber>=0.11
pdf2image>=1.17     # optional — only needed when RAG_ENABLE_PDF_OCR=true (needs poppler)
pytesseract>=0.3    # optional — only needed when RAG_ENABLE_PDF_OCR=true (needs tesseract-ocr)

# ── Reranking (optional) ──────────────────────────────────────────────────────
sentence-transformers>=3.0  # only needed when RAG_USE_CROSS_ENCODER_RERANKER=true

# ── Graph store (optional) ────────────────────────────────────────────────────
neo4j>=5.14          # only needed when RAG_GRAPH_DB_BACKEND=neo4j

# ── Advanced evaluation (all optional) ────────────────────────────────────────
ragas       # optional — RAG_ADVANCED_EVAL_PROVIDER=ragas
deepeval    # optional — RAG_ADVANCED_EVAL_PROVIDER=deepeval
